// FeedbackButton.js
import React from 'react';

const FeedbackButton = ({ onClick }) => {
  return (
    <button className="btn btn-primary" type="button" fdprocessedid="v4c8nk" onClick={onClick}>Give Feedback</button>
  );
};

export default FeedbackButton;
